# Polygon / Massive API key (required for gamma-OI proxy)
POLYGON_API_KEY=

# Optional: FRED API key (pandas-datareader works without it for most series)
FRED_API_KEY=

# Data directory (default: ./data)
DATA_DIR=./data
